# Netlify Configuration for Next.js (App Router)

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Optional: Ensure Node.js version compatibility
[build.environment]
  NODE_VERSION = "20"
